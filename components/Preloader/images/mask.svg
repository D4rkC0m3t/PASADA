<svg width="962" height="192" viewBox="0 0 962 192" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      .text { 
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        font-weight: 800;
        font-size: 140px;
        letter-spacing: -5px;
      }
    </style>
  </defs>
  <!-- Black background (hidden area) -->
  <rect width="962" height="192" fill="#000000"/>
  <!-- White text (visible area) -->
  <text x="481" y="130" text-anchor="middle" class="text" fill="#FFFFFF">PASADA</text>
</svg>
